// jest.config.js
module.exports = {
  testEnvironment: 'node',
  collectCoverage: true, // Corresponds to "Coverage report: Yes"
  clearMocks: true, // Corresponds to "Automatically clear mock calls: Yes"
  // coveragePathIgnorePatterns: ["/node_modules/"] // Good practice to add later if needed
};
